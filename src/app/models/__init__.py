"""
Database models (SQLAlchemy ORM models).

This module will contain database table definitions when database
integration is added to the project.

Future models might include:
- User (for authentication)
- SavedListing (bookmarked properties)
- PriceHistory (historical price data)
- SearchQuery (saved searches)
"""

# Placeholder - database models will be added here when needed
# Example:
# from sqlalchemy import Column, Integer, String, Float, DateTime
# from sqlalchemy.ext.declarative import declarative_base
#
# Base = declarative_base()
#
# class SavedListing(Base):
#     __tablename__ = "saved_listings"
#
#     id = Column(Integer, primary_key=True)
#     url = Column(String, nullable=False)
#     price = Column(Float)
#     surface = Column(Float)
#     created_at = Column(DateTime)

